apiVersion: v1
kind: Service
metadata:
  name: {{ "{{" }} .Service.Name {{ "}}" }}
  namespace: {{ "{{" }} .Namespace {{ "}}" }}
spec:
  selector:
    app: {{ "{{" }} .Service.Name {{ "}}" }}
  ports:
    - name: http
      port: 8080
      targetPort: 8080
